<Page x:Class="CinemaBookingSystem.UI.Pages.CatalogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CinemaBookingSystem.UI.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CatalogPage">

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,100,0,0">
            <ItemsControl x:Name="MoviesGrid" Margin="20">

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>

                        <Button Background="Transparent" BorderThickness="0" 
            Cursor="Hand" Click="MoviePoster_Click">
                            <Border Width="200" Height="350" Margin="15" 
                            CornerRadius="12" 
                            Cursor="Hand">

                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="60"/>
                                </Grid.RowDefinitions>

                                <Image Grid.Row="0" 
                                   Source="{Binding PosterPath}" 
                                   Stretch="UniformToFill" 
                                   Margin="0,0,0,0" />

                                <TextBlock Grid.Row="1" 
                                       Text="{Binding Title}"
                                       FontFamily="Century Gothic"    
                                       Foreground="White" 
                                       FontWeight="SemiBold" 
                                       FontSize="16" 
                                       TextAlignment="Center" 
                                       TextWrapping="Wrap" 
                                       VerticalAlignment="Center"
                                       Margin="10,5"/>
                            </Grid>                                
                        </Border>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>

            </ItemsControl>
        </ScrollViewer>

    </Grid>
</Page>
